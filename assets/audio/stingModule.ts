/**
 * Export outro audio — set to `require('./pulseverse-export-sting.mp3')` after you add the file.
 * See `theme/exportEndCard.ts` for loudness and creative direction.
 */
export const EXPORT_STING_MODULE: number | null = null;
