Parked SQL — NOT applied by `npm run db:push`

181_optimize_get_ranked_feed_v2_ctes.sql
  Feed ranking optimization. Header marks it NOT production-approved until
  output-equivalence + performance validation completes. When approved, move
  back to supabase/migrations/ with the next sequential number (or renumber).
